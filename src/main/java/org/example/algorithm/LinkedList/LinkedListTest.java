package org.example.algorithm.LinkedList;

/**
 * 此家伙是双向队列，实现的是优先级队列, 所以内部可以排序？ 刚刚的case里面没看出来
 * 不过可以在外部判断，选择放在头部和尾部，显得更灵活
 * 也可以从头或者从尾巴拿
 * 但是peek吧，总会只能从一个地方拿 如果要小堆上面就放最小的，要大堆上面就放最大的
 *
 */
public class LinkedListTest {
}
